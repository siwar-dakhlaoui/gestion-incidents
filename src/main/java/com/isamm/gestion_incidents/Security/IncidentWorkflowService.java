package com.isamm.gestion_incidents.Security;

public interface IncidentWorkflowService {
  /*  void assignToAgent(Long incidentId, Long agentId);   // SIGNALE → PRIS_EN_CHARGE
    void startResolution(Long incidentId);               // PRIS_EN_CHARGE → EN_RESOLUTION
    void markResolved(Long incidentId);                 // EN_RESOLUTION → RESOLU
    void closeIncident(Long incidentId);                // RESOLU → CLOTURE
*/}
